class RecommendationModel {
  String title;
  String imageURL;
  String buttonTitle;

  RecommendationModel({
        required this.title,
        required this.imageURL,
        required this.buttonTitle
      });
}